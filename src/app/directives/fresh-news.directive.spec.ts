import { FreshNewsDirective } from './fresh-news.directive';

describe('FreshNewsDirective', () => {
  it('should create an instance', () => {
    const directive = new FreshNewsDirective();
    expect(directive).toBeTruthy();
  });
});
